{
  "name": "xyz",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "npm run sync:plantillas && npm run build:dashboard && npm run sync:plantillas && next build",
    "build:dashboard": "npm --prefix dashboard install --no-fund --no-audit && npm --prefix dashboard run build && rm -rf dashboard/node_modules",
    "sync:plantillas": "node scripts/sync-plantillas.js",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "next": "15.4.4",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.4.4",
    "tailwindcss": "^4"
  }
}
